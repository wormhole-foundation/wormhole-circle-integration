name: Build
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  ethereum:
    runs-on: ubuntu-20.04
    steps:
      - name: Build contracts
      - working-directory: ./evm
      - run: make dependencies
